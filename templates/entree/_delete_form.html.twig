<form method="post" action="{{ path('app_entree_delete', {'id': entree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entree.id) }}">
    <button class="btn">Delete</button>
</form>
